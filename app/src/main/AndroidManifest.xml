<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.neu.madcourse.metu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_metu"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_metu_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MetU">
        <activity
            android:name=".chat.RecentConversationActivity"
            android:exported="false" />
        <activity
            android:name=".explore.ExploringActivity"
            android:exported="false" />
        <activity
            android:name=".chat.ChatActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>